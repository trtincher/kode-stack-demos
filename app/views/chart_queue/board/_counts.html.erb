<p id="queue_counts" class="text-sm text-slate-600">
  <strong class="font-semibold text-slate-900"><%= counts["available"] %></strong> waiting &middot;
  <strong class="font-semibold text-slate-900"><%= counts["claimed"] %></strong> claimed &middot;
  <strong class="font-semibold text-slate-900"><%= counts["done"] %></strong> done
</p>
